version: 2

models:
  - name: metricflow_time_spine
    description: Daily time spine required by dbt Semantic Layer / MetricFlow.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
        data_tests:
          - unique
          - not_null
